import { css } from 'emotion'

const chartWrapper = css`
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  width: 400px;
  height: 400px;
`

const chartStyles = {
  wrapper: chartWrapper
}

export { chartStyles }
